ANTHROPIC_API_KEY=NICE_TRY

# Optional: Upstash Redis for durable rate limiting (recommended for production)
# Create a free database at https://console.upstash.com
# Without these, rate limiting falls back to in-memory (resets on deploy)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
